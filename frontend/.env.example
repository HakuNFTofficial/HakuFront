# Reown/WalletConnect project identifier used by the browser build.
VITE_WALLETCONNECT_PROJECT_ID=replace_with_reown_project_id

# Root CID of the validated 512x512 WebP NFT silhouette directory.
# Required for every frontend deployment; do not leave empty.
VITE_IPFS_PREVIEW_CID=
